// Fins the first item in an array

const names = ["albin", "anu", "emmanuel"];

const getFirstName = (array) => {
    if(array.length > 0) {
        console.log(`First name is ${array[0]}`)
    }
}

// For executing this programe always the time of execution will be constant
// Big O Notattion: O(1) - Constant Time Complexity